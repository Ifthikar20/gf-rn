// Temporarily disabled - requires Expo setup
// TODO: Replace with react-native-sound or configure Expo modules
export const useBackgroundAudio = () => {
  // Disabled for now - background audio will be re-enabled with proper setup
  return null;
};
